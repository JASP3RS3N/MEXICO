# Backend configuration — copy to backend/.env and adjust.
MONGO_URL=mongodb://localhost:27017
DB_NAME=smokehouse
JWT_SECRET=change-me-in-prod
JWT_EXPIRE_HOURS=12
CORS_ORIGINS=*
# Set to false to skip seeding the demo smokehouse catalog on first boot.
SEED_DEMO=true
